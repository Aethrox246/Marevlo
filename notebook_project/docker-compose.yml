version: "3.8"

services:

  backend:
    build: ./Backend
    ports:
      - "${NOTEBOOK_BACKEND_PORT:-8080}:8000"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./Frontend
    ports:
      - "${NOTEBOOK_PORT:-8888}:8888"
    depends_on:
      - backend
    restart: always